const checkId = require('./checkId');
const handleMangooseError = require('./handleMangooseError');
const auth = require('./auth');
const upload = require('./upload');

module.exports = { checkId, handleMangooseError, auth, upload };
